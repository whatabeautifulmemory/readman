# Copy to keystore.properties (gitignored) for local release builds.
storeFile=readman-release.jks
storePassword=CHANGE_ME
keyAlias=readman
# keytool (JDK 17) writes PKCS12, where the key password must equal the store password.
keyPassword=CHANGE_ME
